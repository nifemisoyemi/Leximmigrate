# LexImmigrate — Python dependencies
# Start minimal. We add packages as we implement each feature, so nothing
# unused is installed. Pinned to compatible ranges for reproducibility.

Django>=5.2,<5.3          # LTS, security-patched through April 2028
psycopg[binary]>=3.2      # PostgreSQL driver
django-environ>=0.11      # read settings/secrets from environment (.env)
requests>=2.32            # Monday.com lead sync (cases/monday.py)
stripe>=12                # payments — Stage 3 of checkout flow

# --- Added later, per feature (leave commented until we get there) ---
# django-two-factor-auth   # TOTP 2FA (deferred)
# django-storages[boto3]   # DigitalOcean Spaces (Milestone 2)
# django-anymail[postmark] # transactional email (Milestone 2)
# django-rq  redis         # background jobs / virus scanning (Milestone 2)
# django-auditlog          # audit logging (Milestone 3)
# sentry-sdk               # error monitoring (Milestone 3)
# gunicorn                 # production WSGI server (Milestone 3)